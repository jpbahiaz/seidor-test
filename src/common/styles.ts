const centerFlex = `
	justify-content: center;
	align-items: center;
`

export const displayFlexColumn = `
	display: flex;
	flex-flow: column;
	${centerFlex}
`

export const displayFlexRow = `
	display: flex;
	flex-flow: row wrap;
	${centerFlex}
`